# `schemas` package: Pydantic v2 DTOs that define the API's request/response
# contract (the wire format). Kept separate from `domain.models` so the public
# API can evolve independently of the internal domain representation.
