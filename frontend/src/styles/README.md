# styles/

Global styling lives in `src/app/globals.css` (Tailwind v4 tokens, keyframes,
design-system utilities). This folder is reserved for future standalone
stylesheets (e.g. print styles for the PDF preview, email templates).
